web:
  build: nginx/
  ports:
    - "8000:80"
  links:
    - db
    - php
  volumes:
    - code:/var/www
php:
  image: php5-fpm
  links:
    - db
  expose:
    - "9000"
  volumes:
    - code:/var/www
db:
  image: mariadb
  expose:
    - 3306
